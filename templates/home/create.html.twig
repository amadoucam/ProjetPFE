{% extends 'base.html.twig' %}

{% block title %}
    Creation D'offre
{% endblock %}

{% block body %}
<br/>
<br/>
<br/>
<br/>

<div class="container">

    {% if editMode %}
    <h1 align="center">Modifier d'une offre</h1>
    {% else %}
    <h1 align="center">Création d'une offre</h1>
    {% endif %}

    {{ form_start(formOffre) }}
    <div class="col-sm-6">{{ form_row(formOffre.title, {'attr': {'placeholder' : "Titre de l'offre"}}) }}</div>
    <div class="col-sm-6">{{ form_row(formOffre.categorie) }}</div>
    <div class="col-sm-6">{{ form_row(formOffre.ville) }}</div>
    <div class="col-sm-6">{{ form_row(formOffre.content, {'attr': {'placeholder' : "Contenu de l'offre"}}) }}</div>
    <div class="col-sm-6">{{ form_row(formOffre.image, {'attr': {'placeholder' : "Image de l'offre"}}) }}</div>
    <div class="col-sm-6">{{ form_row(formOffre.createdAt, {'label' : "Date Création"}) }}</div>
    <button type="submit" class="btn btn-info">
        {% if editMode %}
        Enregistrer
        {% else %}
        Ajout Offre
        {% endif %}
    </button>
    {{ form_end(formOffre) }}

</div><br/><br/>
{% endblock %}